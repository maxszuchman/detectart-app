<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.DeviceInfoActivity"
    android:orientation="vertical"
    android:background="@color/colorPrimary">

    <fragment xmlns:android="http://schemas.android.com/apk/res/android"
        android:id="@+id/map"
        android:name="com.google.android.gms.maps.MapFragment"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="70"/>

    <LinearLayout
        android:id="@+id/button_panel"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="30"
        android:layout_marginLeft="10dp"
        android:layout_marginRight="10dp"
        android:orientation="vertical">

        <Button
            android:id="@+id/recomendationsBtn"
            android:textSize="17sp"
            android:text="@string/recomendaciones_seguridad"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="5"
            android:layout_marginTop="7dp"
            android:visibility="gone"/>

        <Button
            android:id="@+id/reNameBtn"
            android:textSize="17sp"
            android:text="@string/renombrar"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="5"
            android:layout_marginTop="7dp" />

        <Button
            android:id="@+id/reAttachBtn"
            android:textSize="17sp"
            android:text="@string/reaparear"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="5"
            android:layout_marginTop="2dp" />

        <Button
            android:id="@+id/deleteDeviceBtn"
            android:textSize="17sp"
            android:text="@string/eliminar"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="5"
            android:layout_marginTop="2dp"
            android:layout_marginBottom="7dp" />

    </LinearLayout>

</LinearLayout>